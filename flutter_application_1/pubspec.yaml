name: cpm_parking
description: Car Parking Monitoring - CPM App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  mobile_scanner: ^4.0.1
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.3
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true